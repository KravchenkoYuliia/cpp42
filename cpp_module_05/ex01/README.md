## Bureaucrat
	wants to sign a form    -> call ft signForm    -> class Form checks is his grade is enough
	wants to execute a form -> call ft executeForm -> class Form checks if it's signed and grade is enough
